/*
 * APP_G_LCD.c
 */ 


#include <avr/delay.h>


int NULL ()
{
	/*
	
			int frequency = 14;
			int duty_cy = 42;

			PWM0_Init();
			PWM0_SetDutyCycle(duty_cy);
			PWM0_Start ();

			GLCD_Init();
			GLCD_ClearAll();
			GLCD_Command(0x80);
			GLCD_String_Letter(2,"Freq: %d Hz");
			//GLCD_String_Letter(3,frequency);
			//GLCD_String_Letter(4," Hz");

			GLCD_Command(0xC0);
			GLCD_String_Letter(5,"Duty: ");
			GLCD_String_Letter(6,duty_cy);
			GLCD_String_Letter(7," %");

			_delay_ms(4000);
	
	if (duty_cy == 0 )
	{
		GLCD_String_IMG(Zero);
		
	}
	
	else if (duty_cy <= 10)
	{
		GLCD_String_IMG(One);
		
	}	
	
	else if (duty_cy <= 20)
	{
		GLCD_String_IMG(Eleven);
	}
	
	else if (duty_cy <= 30)
	{
		GLCD_String_IMG(Twenty_One);
	}
	
	else if (duty_cy <= 40)
	{
		GLCD_String_IMG(Thirty_One);
	}		
	
	else if (duty_cy <= 50)
	{
		GLCD_String_IMG(Forty_One);
	}
	
	else if (duty_cy <= 60)
	{
		GLCD_String_IMG(Fifty_One);
		
	}	
	
	else if (duty_cy <= 70)
	{
		GLCD_String_IMG(Sixty_One);
	}

	else if (duty_cy <= 80)
	{
		GLCD_String_IMG(Seventy_One);
	}
	
	else if (duty_cy <= 90)
	{
		GLCD_String_IMG(Eighty_One);
	}
	
	else if (duty_cy <= 100)
	{
		GLCD_String_IMG(Ninty_One);
	}
	GLCD_String_Letter(2,"LCD Graph For Given   Duty cycle :");

	while(1);
	*/
}